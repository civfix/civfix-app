import { MAP_ACTION_SIZE } from "../shell/expandedFramePlan"

/** The map's glass controls take the width the expanded frame plan reserves for each top-right action. */
export const GLASS_CONTROL_SIZE = MAP_ACTION_SIZE
