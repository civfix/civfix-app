export const DEFAULT_ZOOM = 13
export const FOCUS_ZOOM = 16
export const CAMERA_EASE_MS = 600
export const CLUSTER_FLY_MS = 450

/** A native marker tap also reaches the map's own onPress, so a map press this soon after one is dropped. */
export const MARKER_PRESS_GUARD_MS = 350
