/**
 * The native MMKV instance ids behind `persistentStorage`. Each names the file a store's saved state lives
 * in, so renaming one silently drops every user's saved value.
 */
export const MAP_FILTERS_STORAGE_ID = "civfix.ui.filters"
export const SIDEBAR_STORAGE_ID = "civfix.ui.sidebar"
export const APP_PROMO_STORAGE_ID = "civfix.ui.app-promo"
