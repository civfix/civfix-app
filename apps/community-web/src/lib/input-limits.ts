// These mirror the @civfix/shared schema maxima so a field stops where the server would refuse the
// value. The contract does not export them yet; until it does, this is the one web copy.
export const EMAIL_MAX_LENGTH = 254
export const HTTPS_URL_MAX_LENGTH = 500
export const HANDLE_MAX_LENGTH = 20
