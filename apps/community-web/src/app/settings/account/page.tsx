import { HomeShell } from "@/components/home/home-shell"

/**
 * /settings/account - email, handle, social links, data export and account deletion.
 */
export default function SettingsAccountPage() {
  return <HomeShell />
}
