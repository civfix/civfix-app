import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Blocked accounts · civfix",
}

export default function L({ children }: { children: React.ReactNode }) {
  return children
}
