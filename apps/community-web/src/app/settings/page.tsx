import { HomeShell } from "@/components/home/home-shell"

/** The dedicated route dir is what makes a deep link or refresh work under static export. */
export default function SettingsPage() {
  return <HomeShell />
}
