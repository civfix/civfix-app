import { HomeShell } from "@/components/home/home-shell"

/**
 * /settings/language - the language switcher. Same cold-load gap as /settings/blocked.
 */
export default function SettingsLanguagePage() {
  return <HomeShell />
}
