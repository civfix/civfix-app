import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Notifications · civfix",
}

export default function L({ children }: { children: React.ReactNode }) {
  return children
}
