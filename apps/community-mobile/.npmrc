# Mirror of the workspace-root .npmrc: EAS Build and `expo install` resolve npm config from this app
# directory. node-linker=hoisted is the layout Metro, the config plugins and CocoaPods are proven on.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
@civfix:registry=https://repo.civfix.org/
